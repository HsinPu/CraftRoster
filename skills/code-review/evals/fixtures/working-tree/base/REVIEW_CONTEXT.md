# Change context

The intended code change extracts stock argument validation and adds a local order-preview script with synthetic input. The stock helper has both staged and unstaged edits; review the complete current behavior and account for the index separately.

The edit to `notes/team-schedule.md` was already present before this task. It is a user-owned scheduling note unrelated to stock behavior. Identify it in scope accounting without attributing it to the implementation or reverting it.
