# Fictional partner import incident context

The older CSV producer retained presentation formatting in its item-code cell while the catalogue used uppercase SKU keys. An earlier compatibility change kept those exports within the supported input contract at the importer boundary. The team intended to continue accepting that producer during its replacement rollout.

This note is synthetic historical context, not a real production log or a present-tense validation result. The current caller and contract determine whether the old invariant still applies. Reproduce relevant behavior before treating historical intent as a current finding.
